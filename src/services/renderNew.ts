export {};
/*
renderNew(setCocktailData,setPageNumber,data,message)
const renderNew = (setCocktailData:any, 
    setPageNumber:React.Dispatch<React.SetStateAction<[number, number]>>,) => {

    return data;
  };
  
  export default renderNew;
  */
